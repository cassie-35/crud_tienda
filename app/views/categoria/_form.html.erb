<div class="form-container">
  <%= stylesheet_link_tag 'cyp', media: 'all' %>

  <%= form_with(model: categorium) do |form| %>
    <% if categorium.errors.any? %>
      <div class="notice">
        <h2><%= pluralize(categorium.errors.count, "error") %> prohibited this categorium from being saved:</h2>
        <ul>
          <% categorium.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :nombre %>
      <%= form.text_field :nombre %>
    </div>

    <div>
      <%= form.label :descripcion %>
      <%= form.text_area :descripcion %>
    </div>

    <div>
      <%= form.label :fecha_de_creacion %>
      <%= form.date_field :fecha_de_creacion %>
    </div>

    <div>
      <%= form.label :estado %>
      <%= form.text_field :estado %>
    </div>

    <div>
      <%= form.label :imagen %>
      <%= form.text_field :imagen %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  <% end %> 
</div>
